<h2>My TODOs</h2>
<table>
	<tr>
		<th>Description</th>
		<th>Created at</th>
	</tr>
	<% if @todos.any? %>
		<% @todos.each do |todo| %>
		<tr>
			<td><%= link_to todo.description, todo %></td>
			<td><%= todo.created_at %></td>
		</tr>
		<% end %>
	<% else %>
			<p>There are no TODOs.</p>
	<% end %>
</table>