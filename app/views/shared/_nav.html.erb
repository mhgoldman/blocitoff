<ul class="nav nav-tabs">
	<%= nav_link 'Blocitoff', root_path, a_class: 'navbar-brand' %>
	<%= nav_link 'About', welcome_about_path %>
	<% if user_signed_in? %>
		<li><%= nav_link 'My TODOs', todos_path %></li>
		<li><%= nav_link 'Add TODO', new_todo_path %></li>
		<li class="navbar-right dropdown">
			<a class="dropdown-toggle" data-toggle="dropdown" href="#" role="button" aria-expanded="false"><%= current_user.email %> <span class="caret"></span></a>
			<ul class="dropdown-menu">
				<%= nav_link "Log Out", destroy_user_session_path, method: :delete %></li>
			</ul>
		</li>
	<% else %>
		<%= nav_link 'Log In', new_user_session_path, li_class: 'navbar-right' %>
		<%= nav_link 'Sign Up', new_user_registration_path, li_class: 'navbar-right' %>
	<% end %>
</ul>